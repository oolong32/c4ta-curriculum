  <%- include('../includes/header') %>
  <h2><%= title %></h2>

  <form method="POST" action="">
    <label for="name">Vorname</label>
    <input type="text" name="first-name" id="first-name" value="<%= !teacher ? '' : teacher.firstName %>" required="true">

    <label for="name">Nachname</label>
    <input type="text" name="last-name" id="last-name" value="<%= !teacher ? '' : teacher.lastName %>" required="true">

    <button type="submit">erfassen</button>
  </form>

  <% if (errors) { %>
    <ul>
      <% for (error in errors) { %>
        <li><%= error.msg %></li>
      <% } %>
    </ul>
  <% } %>
  
  <%- include('../includes/footer') %>